CONSIDERO QUE ESTA FUNCION ES DE ALTA IMPORTANCIA YA QUE PUEDE EMPLEARSE EN LA PREVISUALIZACIÓN DE ALGUN CLIENTE, TENIENDO EN CUENTA ESTOS DATOS BASICOS DE CONTACTO AL MOMENTO DE REALIZARSE UNA COMPRA.
ESTA FUNCION LLAMA AL ID DE CLIENTE, EL NOMBRE Y EL NUMERO DE CONTACTO, YA SEA PARA UNA CONFIRMACIÓN DE VENTA O ALGUNA BREVE INFORMACIÓN DE ESTE MISMO.

delimiter //
CREATE FUNCTION obtener_datos_cliente(id_cliente INT)
RETURNS VARCHAR(255)
DETERMINISTIC
BEGIN
    DECLARE resultado VARCHAR(255);

    SELECT CONCAT(id_cliente, ', ', nombre, ', ', contacto)
    INTO resultado
    FROM clientes
    WHERE id_cliente = id_cliente
    ORDER BY id_cliente DESC
    limit 1;

    RETURN resultado;
END //
